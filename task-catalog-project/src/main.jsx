import React from "react";
import ReactDOM from "react-dom/client";
import TaskCatalog from "./TaskCatalog.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <TaskCatalog />
  </React.StrictMode>
);
